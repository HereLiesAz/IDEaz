<?xml version="1.0" encoding="utf-8"?>
<!--
   Backup rules for devices older than API 31.
   Controls what is backed up via Auto Backup.
   See https://developer.android.com/guide/topics/data/autobackup
-->
<full-backup-content>
    <!-- Include all files and shared preferences by default -->
    <include domain="file" path="."/>
    <include domain="sharedpref" path="."/>

    <!-- Exclude temporary build caches -->
    <exclude domain="file" path="cache"/>

    <!-- Exclude build artifacts -->
    <exclude domain="file" path="web_dist"/>

    <!-- Default-shared-prefs file holds GitHub PATs, Google API keys,
         Jules API keys, and keystore passwords. Excluded from Auto Backup. -->
    <exclude domain="sharedpref" path="com.hereliesaz.ideaz_preferences.xml"/>
</full-backup-content>
